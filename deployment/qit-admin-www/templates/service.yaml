apiVersion: v1
kind: Service
metadata:
  name: {{ include "qit-admin-www.fullname" . }}
  labels:
    {{- include "qit-admin-www.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qit-admin-www.selectorLabels" . | nindent 4 }}
